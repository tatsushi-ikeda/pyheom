[pytest]
testpaths = tests benchmarks
markers =
    unit: pure-Python tests, no compiled pylibheom required
    integration: requires built pylibheom extension
    gpu: requires CUDA backend
    benchmark: solver performance benchmarks (requires pytest-benchmark)
addopts = --tb=short -ra
